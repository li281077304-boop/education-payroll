# 2026-08 real Payroll UAT evidence (2026-09-16)

This is a fresh, isolated production-path exercise. It uses the existing
Payroll service and the real files under `/Users/macos/Desktop/8月工资表`.
The original shared Payroll workspace was not changed.

## Run and period

- isolated service run: `18a38d63ebcd`
- period label: `2026-08`
- period boundary: `2026-08-03` through `2026-08-30`
- boundary source: `SOURCE_FILE_RANGE`
- mode: `GENERATE`

## Inputs and hashes

| role | source | SHA-256 prefix | observed result |
|---|---|---|---|
| schedule | `排课列表_08月03日到08月30日_202609011519.xls` | `da6efe21c8712762...` | 2,385 rows / 51 teachers |
| payroll baseline / G-L | `2026数学&理化组8月薪资表——基准版.xlsx` | `d0cbb9c848fabf59...` | 31 teacher rows; blank G-L cells remain missing |
| renewal source | `2026年度续费+推荐数据(3).xlsx` | `9c498402652f2709...` | 49 rows from the real `8月` sheet |
| refund source | `宣城二校退费统计表2026年.xls` | recorded by package scanner | 31 rows from `8月份 `; status column is blank |

## Current fresh-run results

- AA: `51/51 DETERMINED`
- AC: `51/51 DETERMINED`
- AD: `51/51 DETERMINED`
- AE: `51/51 DETERMINED`
- AF: `6 DETERMINED`, `45 ESTIMATED`; a fresh AF monthly confirmation is still required and no prior Run confirmation was reused.
- M: `19 DETERMINED`, `32 BLOCKED_BY_INPUT`. The source workbook's blank G-L cells are preserved as missing; they are not converted to zero.
- M comparison for the 19 determined rows: `19/19 MATCH` against the same
  real August baseline workbook; this does not promote the 32 missing rows.
- schedule / grade processing: 2,385 records loaded; the existing grade and period evidence remains attached to the Run.

## Source-gap decisions

The 8月 renewal workbook is now read from the actual `8月` sheet through the
existing business-result adapter. Its rows are `SUBMITTED`, have no explicit
`APPROVED` review state, and carry display names rather than a separately
authoritative teacher ID. They are therefore not bound to payroll and remain a
business/identity confirmation item, not a silent zero.

The refund workbook is readable from the real `8月份 ` sheet (31 rows), but
its `状态` field is empty. It cannot be treated as an approved refund result.

The remaining AV components (AG, AL, AM, AN, AO-AU and the derived AV) remain
blocked by missing authoritative business rules or approval evidence. The
current export is intentionally `NEEDS_CONFIRMATION`; it is not claimed as a
final payroll.

## Export and deterministic checks

- normal generated artifact: `/Users/macos/Desktop/Payroll-UAT-2026-08-18a38d63ebcd.xlsx`
- export status: `NEEDS_CONFIRMATION` with explicit blockers; no values were
  fabricated to force AV completion
- `uv run --with '.[test]' pytest -q`: **374 passed**
- `uv run python -m compileall -q payroll_core payroll_ui`: **PASS**
- `git diff --check`: **PASS**

The local Payroll UI was also started against the same isolated data store
(`python -m payroll_ui --data-dir /tmp/payroll-prod-uat-20260916 --no-browser`),
served a real page, and returned the same fresh Run through its authenticated
`/api/runs/index` and `/api/runs/18a38d63ebcd` endpoints. The server was then
stopped cleanly; no Chrome or source workbook was touched.

The fresh Run therefore has no remaining technical-open item. Its remaining
items are explicit business/source confirmations: AF monthly policy, missing
G-L inputs for 32 teachers, approval and identity binding for renewal results,
refund approval status, and rules/sources for other AV components.
