# V1 RC 最终验收（2026-09-10）

本轮基线为 `9592d8b`，分支为 `feature/payroll-reliability-audit`。
未修改工资计算规则、真实 Excel 或 main。本文仅含脱敏汇总。

## 实际 UI 验收

- 恢复已有真实 2026-08 Run，没有重新导入资料或新建真实 Run。
- 核算范围 27 人；AA 27 一致，星级 27 一致，公式完整性通过。
- AC 25 一致、2 条差异；AE、AF 各 2 条差异。
- 6 条原始异常完整保留，显示为 4 个业务问题：2 个 AC，2 个 AE/AF 聚合问题。
- 逐一打开全部 4 个问题。AC 展示系统值、目标值、差值及逐课来源、工作表、单元格、班型、年级和折算依据；AE/AF 在同一详情展示共同输入、分别命中的生效规则、个人政策、特批、义务小时和目标来源。
- 从异常中心进入 AE/AF 详情为 1 次点击；AC 详情加展开逐课明细为 2 次点击。从核对结果页计入进入异常中心，仍不超过 3 次。
- 在真实 UI 中分别对 1 个 AC 和 1 个 AE/AF 问题保存 DEFERRED 意见。聚合问题只处理一次，同时绑定两个字段。没有通过数据库写入伪造 UI 操作。
- 在真实 UI 重跑全部材料，意见保持有效；关闭浏览器页并停止应用进程后重新启动，Run、4 个问题和两条意见均恢复。
- 在单独的人工构造测试实例中通过 UI 保存意见，再改变测试排课副本的文件版本。UI 显示文件已变化、1 条人工决定需要重新确认，并禁用旧核对结果及开始核对按钮。真实 Excel 未参加变更测试。

## 决定与审计边界

业务问题层不删除或改写原始字段审计。决定绑定 Run、稳定根因键、影响字段、来源哈希、星级/政策版本、档位规则和问题事实指纹。
相同输入重跑保留意见；输入或依据变化后保留旧意见并要求重新确认。重新确认前的意见进入历史。
人工意见是处理记录，不直接把工资值改成正确，也不直接令总体通过。

AE 与 AF 分别使用自己的权威星级或个人政策选择规则，不能互相替代；AD 仍来自工资表，详情明确未形成完全独立闭环。
AE/AF 聚合采用结构化字段关联：同一教师的单价差异与总额差异在两端对应相同计费小时，才合为共同依据问题；不能证明关联的政策差异保留独立卡片。

## 剩余业务问题

仍有 4 个问题未获负责人最终裁决：2 个已记录暂缓、2 个尚无人工意见。
验收意见不代表确认错误或批准例外。真实工资 Run 保持 REVIEW_REQUIRED，而不是工资核对 PASS。
软件 RC 验收与本月工资业务审批是不同结论。

## 安全

真实输入文件的 SHA-256 与 Run 注册值全部一致。真实数据、运行数据库和意见仅保留本机；本轮提交只包含 UI/服务层代码、人工构造测试和本说明，不含真实教师、学生、工资或课程明细。
远端仓库已核验为 Private，不合并 main。

## 回归结果与裁决

完整 pytest：72 passed，0 failed。覆盖聚合保留原始记录、单次决定绑定 AE/AF、HTTP 保存路径、同输入重跑、重新打开、源/目标哈希失效、规则版本失效、证据选用正确权威和界面呈现。

本轮限定范围的软件验收：V1 RELEASE CANDIDATE = YES。
此结论不表示 8 月工资通过，也不表示 AE/AF 已完全独立闭环。
