# DEVLOG

## 2026-09-11 — 年级历史窗口与毕业边界

- 普通历史课表年级推断正式限制为目标工资月向前 12 个自然月的包含窗口；更早记录不会参与自动结论或制造冲突。
- 人工确认仍是带日期的权威事实，不受普通课表窗口限制，但继续按 9 月边界推算。
- 去除“高三永久封顶”行为：跨过高三后的 9 月，历史证据失效并要求人工确认，不再虚构新的学历年级或继续标为高三。

## 2026-09-11 — 历史课表年级证据迁移

- 赠送／换购／特批课程不再要求普通用户每月维护学生年级表：系统会保存正常历史课程中的最小年级证据，并按课程发生日期推断当前工资月份的年级。
- 9 月 1 日被正式定义为自然升年级边界；普通学段跨边界自动升一级，领航伴学、雅思、托福等不自动升级。
- 同一学生的历史证据只有推算后完全一致才自动使用；不同导出版本冲突时明确进入 `NEEDS_INPUT`，不按最新导出覆盖旧事实。
- 人工确认作为带日期、可追溯的本地事实保存；原 `student_grade_lookup.csv` 保留为迁移兼容来源，而非日常必填输入。

## 2026-09-11 — Skill 到 AA / AC 清洗链迁移审计

- 对照当前工资核对 Skill、排课 Adapter 与配置化 Core，确认“实到 = 0 不参与课时计算”此前已在 Core 正确实现；本轮新增 Adapter 与 Core 回归保护，防止 0 被误转为缺失或 UNKNOWN。
- 找到并迁移一个真实遗漏：Skill 对赠送／换购／特批课程会查本地学生年级权威表，Core 过去只从班级名提取年级，导致这类课程无法计算。
- 修正泛称“一对多”直接映射为 1对2 的历史行为：只有课程名称明确标记 1v2/1对2 或 1v3/1对3 时才进入对应特殊班型；否则明确要求补资料。
- 明确工资所属月份用于筛选排课日期，跨月课程不再被误算进本月。
- 真实只读复核发现：本组原先缺年级课程可由既有本地权威表补齐；实到为 0 的课程并非 UNKNOWN 根因。剩余差异维持为业务差异，未调整公式凑数。

## 2026-09-11 — v0.21.0-rc1 Mac 主线收口

- 将新 Run 的核心计算收敛为唯一权威链：排课 → AA → AC → AD → AE → AF；AD 固定由 AA + AC 得出，提交工资表只参与核对。
- 普通小班明确按实到人数走稳定 Core：1 人 0.8、2 人 1.0、3 人 1.2 等；一对一继续留在稳定 AA 主核。
- 废弃“特殊班型固定单一系数”模型。特殊班型改为版本化的“班型 + 实到人数 → 系数”：1对2 为 1 人 0.8 / 2 人 1.2；1对3 为 1 人 0.8 / 2 人 1.2 / 3 人 1.5。
- 普通小班实到 2/3 人不再被识别成 1对2/1对3；特殊班型缺少对应人数配置时明确报缺资料，不回退到普通小班规则。
- 新 Run 统一绑定完整核心规则包；历史班型规则入口仅保留只读兼容说明，避免出现双重权威。
- 保留并验证星级的权威/参考/缺失状态，个人工资政策优先于角色默认，兼职与无课管理岗走独立处理。
- Adapter、历史 Run 规则快照、Generate/Audit 共用 Core 的回归均已覆盖。
- `v0.21.0-rc1` 进入功能冻结与 Windows 真实机 UAT 阶段；本轮不做 Windows 打包或新的工资业务模块。

## 2026-09-10 — 跨 Agent 交接整理

- 历史 macOS V1 RC 实机验收已通过：真实 2026-08 Run 可恢复，27 人范围、AA、星级、公式完整性、AC 证据、人工决定持久化与失效机制已经在此前工作中验证。
- `3efd8e1` 增加了基础资料修正版 workflow，以及 AC 逐课计算证据和批注结构化展示。
- 真实 UAT 发现一条班课差异：独立排课计算与工资表相差 0.38；该问题指向 8 月升年级后排课系统年级未同步，不能简单归为工资表错误。
- 业务上明确区分两类处理：`SOURCE_DATA_CORRECTION` 修正上游事实的有效副本；`APPROVED_PAYROLL_OVERRIDE` 针对明确课程和本 Run 的特殊核算口径。二者都不得覆盖原始排课事实。
- 之所以新增结构化 resolution workflow，是为了让差异经反事实重算后可验证闭合，并让人工决定绑定稳定业务问题，而不是停留在“接受例外”文字上。

## 既有背景

历史文档和提交记录显示，项目已从 Skill 驱动的散落脚本逐步增加独立核对、来源追溯、Coverage/PASS 门禁、Business Issue 聚合和本地 UI。本文档记录演变原因；当前可执行状态以 `CURRENT_STATE.md` 和仓库测试为准。
