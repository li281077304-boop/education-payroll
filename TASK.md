# TASK — AA / AC 真实 UAT（v0.21.0-rc1 审计修正版）

## 目标

使用真实课表只读验证“排课 → AA / AC → AD”逐课闭合。先确认学生年级权威查表位于本机应用数据目录 `config/student_grade_lookup.csv`；迁移期如当前 Skill 仍存在同名资料，应用会只读使用它，不复制进仓库。

## 开始前

1. 阅读 `AGENTS.md`、`CURRENT_STATE.md`、本文件与最新 `DEVLOG.md`。
2. 确认分支、提交、工作区干净状态；不要基于未提交的 resolution workflow 半成品。
3. 使用 Python 3.11 x64、独立 venv 和脱敏 fixtures。真实业务数据只允许本机只读使用，不能进入 Git 或安装包。

## 必须验证

- 逐课抽查至少一名 AA 差异教师与两名 AC 差异教师：确认有效状态、实到、班型、年级、规则、单条贡献与合计。
- 确认实到为 0 的课程不进入 AA/AC/AD；赠送／换购／特批课程有权威年级时可计算，无权威时明确报缺资料。
- 确认泛称“一对多”只有在课程标题明确 1v2/1v3 时才使用特殊班型规则。
- 确认工资所属月份过滤跨月课程；提交工资表 AA/AD 如无公式缓存，只标记为不可可靠比较，不反向影响 Core。

## 禁止事项

- 不改变 AA/AC/AD/AE/AF 业务规则，不合并 main，不扩外围工资项目。
- 不接续费、退费、推荐、管理绩效、AV，不触碰 Source correction / approved override 开发线。
- 不提交真实工资、姓名、学生、排课、续退费数据或真实 Excel。

## 完成标准

每一条保留的业务差异都能在 UI 中看到源课表位置和逐条计算过程；不能用总数调参或工资表数值倒推规则。
