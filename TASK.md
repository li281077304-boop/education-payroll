# TASK — 学生年级补齐真实 UAT

## 目标

使用真实课表只读验证学生年级补齐：历史正常课表、已保存人工确认与当前课程班名能按正确优先级得出可解释年级，并让受影响的 AA/AC 逐课闭合。普通用户不再需要维护学生年级 CSV；本机 `config/student_grade_lookup.csv` 与迁移期 Skill 同名表仅是兼容兜底。

## 开始前

1. 阅读 `AGENTS.md`、`CURRENT_STATE.md`、本文件与最新 `DEVLOG.md`。
2. 确认分支、提交、工作区干净状态；不要基于未提交的 resolution workflow 半成品。
3. 使用 Python 3.11 x64、独立 venv 和脱敏 fixtures。真实业务数据只允许本机只读使用，不能进入 Git 或安装包。

## 必须验证

- 用本机真实 8 月排课导出只读验证：已有学生年级历史能纠正 9 月 1 日导出造成的班名提前升年级。
- 已只读抽查：一名 AC 教师已由历史年级纠正回预期值，另一名 AA 教师保持原系统值不变。两名 AC 教师仍存在学生历史自身冲突，应通过新的按学生确认页补齐后再完成目标值验证；提交工资表少计的差异仍是工资表遗漏而不是 Core 修正目标。
- 验证历史上传可连续添加、缺年级按学生聚合、手动确认一次会重算该学生所有相关课程，并在重启后保留。
- 验证 2026-09-19 不升年级、2026-09-20 起升年级；无法可靠推断时只影响相关课程，明确提示确认，不阻塞无关教师。
- 验证多人历史反证不会被当前班名吞掉：部分冲突与学生间冲突均待确认，完整一致历史才覆盖班名。
- 验证同一旧课程在 9 月后重复导出造成的单级班名升级会自动消解，不写入新的永久学生事实。

## 禁止事项

- 不改变 AA/AC/AD/AE/AF 业务规则，不合并 main，不扩外围工资项目。
- 不接续费、退费、推荐、管理绩效、AV，不触碰 Source correction / approved override 开发线。
- 不提交真实工资、姓名、学生、排课、续退费数据或真实 Excel。

## 完成标准

真实课表的年级污染被可靠历史证据纠正，后续导出造成的班名污染不制造假冲突；缺失学生可通过两种用户入口完成补齐；不得用工资表数值倒推年级或调整 AA/AC 公式。
