# CURRENT_STATE

更新日期：2026-09-11

## 当前版本

- 版本：`v0.21.0-rc1`
- 当前审计分支：`feature/payroll-aa-ac-audit`（基于 `v0.21.0-rc1`）
- 测试：`247 passed`（年级补齐的 9 月 20 日边界、多人名单、历史上传、人工确认与后续导出污染消解已纳入）。
- 阶段：年级补齐产品流程完成；真实 `2026-08` 只读抽查已确认历史覆盖能纠正至少一处提前升年级班名，但仍有历史事实冲突需人工确认，不能把该次真实核算宣称为全部通过。

## 当前主线能力

- 唯一核心计算链为：排课 → AA → AC → AD → AE → AF；其中 `AD = AA + AC`。
- Submitted Payroll 的 AD 仅是核对目标，不能反向参与 Core 计算。
- 一对一是稳定 AA 主核；普通小班是稳定 AC 主核，按实到人数计算。
- 特殊班型按“班型 + 实到人数”从版本化核心规则包读取：当前确认 1对2（1 人 0.8、2 人 1.2）与 1对3（1 人 0.8、2 人 1.2、3 人 1.5）。
- 权威星级可令 AE 为 `DETERMINED`；提交表星级只能令 AE 为 `ESTIMATED`；无星级为 `NEEDS_INPUT`。
- AF 读取教师个人工资政策及特批，不从 TRMT、组长等角色名称直接推断。
- 兼职按教师/年级的单节课价独立计算，不进入全职星级/义务课时链；明确允许无授课活动的管理岗不会被误报。
- Audit 与 Generate 使用同一套 Core；历史 Run 绑定自己的规则快照，不随以后版本变化。
- Excel Adapter 仍支持已知模板、语义映射、表头别名、列顺序变化、手工映射和 `.xls` 特殊处理；真实数据不进入仓库。
- 课表清洗继承 Skill 的“实到为 0 不参与计算”规则；赠送／换购／特批等无年级课程优先从已保存的人工确认和历史正常课程的日期化证据补齐。可靠学生历史可覆盖受 9 月导出污染的当前班名；每年 9 月 20 日起才自动升年级。旧本地学生年级表仅保留兼容兜底。泛称“一对多”不会再被擅自当作 1对2。
- 多学生课程不会再因班名存在而吞掉历史反证：部分反证或学生间历史冲突会进入待确认，只有历史完整一致或历史与班名一致时才自动继续。
- 对同一旧课程的后续导出，如果能由导出时间和 9 月 20 日规则解释为班名提前升一级，系统会标记并忽略污染记录，避免重复导入不断制造假冲突。

## 已知限制与未完成项

- 本版本不扩展续费、退费、推荐、陪读、管理绩效、AV 或 Windows 打包。
- 普通小班和一对一当前被视为稳定 Core 规则；只有特殊班型、年级系数、AD 档位、星级加成、个人政策与兼职单价进入版本化资料。
- Source-data correction / approved payroll override 仍在独立开发线，不是本次 rc1 的新增范围。
- Windows 兼容和打包尚未验收；必须在 Windows 10/11 实机验证源码与打包产物，不能由 Mac 交叉构建结论代替。
- 非两小时课程与源系统真实重复记录尚无已确认自动处理规则，必须明确提示，不能猜测。
- 真实 `2026-09-01` 导出课表的学生年级覆盖已经本机只读抽查：多人名单已可逐名匹配历史；仍有部分学生的历史事实冲突，必须经 UI 的按学生确认后才会参与最终金额。任何未解决年级只影响对应课程，不应阻塞无关教师的 AA/AC。

## 安全边界

- 不提交真实工资、教师收入、学生姓名、排课、续费退费明细或真实 Excel。
- 不覆盖原始 Excel；所有输出必须新建文件。
- 原始业务事实不可覆盖；规则不明确时应显示缺资料，而不是猜测或归零。
