# Payroll 手工 UAT 记录

- 日期：2026-09-26
- 类型：真实工资助手界面 UAT
- 状态：**PARTIAL**（本轮已修复并验证的部分见文末收口表；仍缺真实界面点击的项目不写 PASS）
- 当前应用：2026-08 核算记录，UAT 时界面显示 31 个待处理问题
- 隐私：按项目规则脱敏；本文件不记录真实教师姓名、工资金额、原始文件路径或工资表内容。
- 状态口径：PASS / PARTIAL（已实现但缺真实 UI 点击）/ OPEN / BLOCKED

## 原始观察整理

### 1. 基本工资应支持整表导入

当前逐格录入基本工资不可用，也不符合真实工作方式。用户已有基本工资表，应通过一份历史工资数据表导入教师基本工资。可用来源包括支持部门提供的标准工资表，或上个月教学部的工资表；两类表的结构相同，界面统一提供“历史工资数据”导入入口即可。没有匹配到的教师再逐项提示补充，不应要求全体教师逐格录入。

### 2. 核对问题必须能进入实际处理

工资公示异常和“需确认”状态目前对用户不够可解释。点击“处理”应直接到对应确认表单，并显示当前问题、需要做的决定和保存反馈。

本次 UAT 中，用户完成了义务课时默认规则及一位匿名教师的例外确认，设置了默认 30 小时、例外 0 小时和原因“带组且没有星级”，随后保存默认规则、例外和确认。用户认可这段流程。

默认 30 小时是惯例规则，不应标为“本月例外”。例外应与默认规则分开、可追溯。此项已有确认的业务输入，施工时不得改动星级加成或个人政策算法。

### 3. 所有操作都要有即时反馈

点击后无反应不可接受。异步操作开始时，按钮应显示处理中并禁用，或出现清楚的进度提示；完成后显示成功，失败时显示可操作的错误信息。UAT 特别观察到“暂不录入，先生成工资表”点击后没有可见反馈。

### 4. 工资预览需要收短备注

预览页备注过长，影响快速检查。保留必要状态与异常原因，详细说明应折叠或按需查看；不要在每一行重复大段提示。

### 5. 模板缺失提示应给出直接入口

生成工资表时出现“尚未设置工资模板，请先到基础资料中设置一次”，但当时没有明确跳转入口，也找不到导航。缺模板时应提供直接跳转/继续设置按钮，设置保存后能返回原流程继续生成。

用户提供的真实公司模板可以只有表头行；模板录入时不得要求提交一份带历史工资数据的旧工资表。模板列、星级来源等尚未验证的地方只做清楚说明，不得改变星级加成或个人政策。

### 6. 工资周期异常需要核实真实覆盖范围

用户已导入 2026-08-01 至 2026-08-30 的课表和续费数据，但界面仍提示排课周期异常。必须对照本 Run 实际导入材料的日期范围与核算周期定位校验原因。不能仅凭文件名或硬编码的月初/月末判断异常。

### 7. 续费材料应出现在当前 Run 的核对结果中

用户表示续费已经导入，预期在本 Run 查看续费核对结果；页面却显示“查看其他项目”，随后续费内容未按预期出现。需要检查材料类别识别、问题聚合和页面导航状态，确认已导入续费数据关联到正确月份、教师和当前 Run。

### 8. 星级规则的查看入口和来源需说清楚

用户不清楚当前教师星级从哪里确定、42/43 等显示值代表什么、如何查看依据。当前阶段只要求让来源、当前值、规则入口和待确认状态清楚可见；不要改动星级加成或个人政策。

## 验收合同

1. 一张兼容结构的历史工资数据表可批量导入基本工资；只有未匹配项需要补录。
2. 默认义务课时和教师例外可从核对问题直接进入、保存、确认并看到状态反馈；惯例规则不标成月度例外。
3. 异步按钮都有处理中/成功/失败的可见状态，并能阻止重复点击。
4. 预览备注简明，详细原因按需展开。
5. 缺模板时有明确的一步设置入口；保存后可返回并继续生成。
6. 核算周期提示与真实导入数据日期一致，不误报完整覆盖的 8 月周期。
7. 当前 Run 已导入的续费可进入续费核对视图，类别、月份、教师匹配状态可解释。
8. 星级值及其数据来源可发现；星级加成和个人政策算法保持不变。
9. 不改写原始工资表或原始课表；所有更正均留在本地 Run 的可追溯数据中。
10. 真实 UI 回归通过，且定向自动化回归通过；任何未解决项继续标为 OPEN，不伪称 UAT PASS。

---

# 结果回填（2026-09-26 收口）

| # | 项目 | STATUS | ROOT_CAUSE | FIX | TEST |
|---|---|---|---|---|---|
| UAT-01 | 基本工资整表导入 | PARTIAL | 只能逐格录入，没有整表识别与导入链路 | 只读识别 G～L + 教师匹配 + 预览 + 确认后才写入当前 Run；未匹配教师保留补录；来源 SHA-256 校验；重名/缺列/无缓存公式 fail closed；空白 H/J 仅在同排 M 公式与缓存共同证明为 0 时按 0；已保存资料下月复用 | `test_base_salary_import.py`（10 项）、`test_payroll_20260926_uat.py` |
| UAT-02 | 问题→确认表单→保存反馈 | PASS | 本轮真实 UAT 中义务课时默认 + 教师例外流程可完成，未发现阻断 | 未改动（属已认可行为）；未改动星级加成 / 个人政策 / 公式 | 本轮真实 UAT（默认 30 小时 + 1 位教师例外 0 小时 + 原因，保存并确认成功） |
| UAT-03 | 异步操作即时反馈 | PARTIAL | 「暂不录入，先生成工资表」只保存 defer 决定并跳页，没做核算；按钮文案与实际动作不一致 | ①一次点击内完成：保存 defer → 自动重新核算 → 生成工资预览；有阻塞进异常处理，无阻塞直达预览；M/AV 保持待补充（绝不按 0）②文案改为「暂不录入，先生成工资预览」③登记专用处理中文字 | `test_defer_base_salary_chain.py`（8 项）、`test_button_feedback_ui.py`（6 项） |
| UAT-04 | 预览备注收短 | PARTIAL | 行内说明偏长，影响快速核对 | 主表保持 7 个快速核对字段；续费/星级/依据收进行内折叠 + 页级折叠「本期使用的依据」 | `test_period_authority_ui.py` |
| UAT-05 | 缺模板给直接入口 | PARTIAL | 缺模板时只有一句提示，没有入口也找不到导航 | 最终生成时缺模板 → 直接进入公司模板设置；保存后返回原 Run 工资预览并可继续生成 | `test_uat_night_regression.py::test_production_export_requires_the_company_template`、`test_button_feedback_ui.py` |
| UAT-06 | 工资周期误判 8/30 | PARTIAL | 工资月份被默认当成自然月（8/1～8/31），课表覆盖到 8/30 → `incomplete_tail` → 导出被 `PERIOD_COVERAGE_INCOMPLETE` 阻断，再要求人工确认 8/30。真实证据：9/17 曾人工确认过该月实际周期，但没有可复用记录，10 天后新 Run 又退回自然月 | 工资月份 → 该月「人工月 / 工资周期」authority → 用它筛选并判断完整性；课表日期只校验人工月，文件名仅低优先级；有人工月时不用自然月月末；超出人工周期才提示越界；无人工月时回退自然月并明确标注；人工确认一次即形成可复用记录；原「确认实际排课周期」降为例外修正 | `test_period_authority.py`（16 项）、`test_period_authority_http.py`（3 项）+ **真实数据复算** |
| UAT-07 | 续费材料与工资结果绑定 | PARTIAL | 旧识别链把年度续费文件的 1 月表当 8 月资料；把“有续费资料”误报成“续费已进工资”；保存记录只有续费人数，缺 AH/AI/AJ 合计 | 只读工资月份对应工作表；界面区分「续费资料已导入」与「续费已确认并进入本次工资」；预览 AH/AI/AJ 后人工确认才写入 snapshot，AK 由确认后的 AH/AI/AJ 计算 | `test_payroll_20260926_uat.py`、`test_renewal_production.py` |
| UAT-08 | 星级来源可见 | PARTIAL | 用户看不到星级从哪确定、42/43 代表什么 | 预览新增折叠「本期使用的依据」显示星级 authority 名称/版本/生效期/来源；基础资料新增「教师星级来源」卡片。星级值与加成算法未改动 | `test_period_authority_ui.py` |
| UAT-09 | 基础资料分层 | PARTIAL | 低频工程配置与月度流程混在一起 | 外层只留：公司模板 / 人工月 / 历史基本工资 / 教师星级来源；星级加成、个人政策、7 类核心规则、班型折算、历史兼容收进「高级设置（管理员）」折叠区。未重构计算引擎 | `test_period_authority_ui.py` |

## 真实数据操作记录

1. **写入前先备份**：把应用数据目录的 `payroll-ui.sqlite3` 完整复制到 `backups/`（带时间戳）。
2. **恢复 2026-08 人工月**：先 dry-run 从历史 Run 的已确认窗口得到候选，再记录 `2026-08 = 2026-08-01 ～ 2026-08-30`（来源：人工月资料；确认人：本机核算负责人（2026-09-26 手工 UAT）），证据中保留 9/17 实际排课范围确认与候选 Run 引用。
3. **把当前 8 月 Run 绑到人工月并复算**：

| | 绑定前 | 绑定后 |
|---|---|---|
| 核算周期 | 2026-08-01 ～ 2026-08-31 | 2026-08-01 ～ 2026-08-30 |
| 周期来源 | LEGACY_CALENDAR_DEFAULT（自然月） | MANUAL_PERIOD_RECORD（人工月资料） |
| 课程日期范围 | 08-03 ～ 08-30 | 08-03 ～ 08-30 |
| incomplete_tail | **true**（误判） | **false** |
| outside_period / outside_authority | false / — | false / false |
| 排课记录数 | 2385 | 2385（不变） |

自动核算后：`PERIOD_COVERAGE_INCOMPLETE` 与 `PERIOD_OUTSIDE_AUTHORITY` 均不再出现；M 仍为「待补充」（`BLOCKED_BY_INPUT`，未被写成 0）。会计周期历史新增一条可追溯记录。

> 口径待确认：本轮记录为 **8/1 起**（依据 9/26 UAT 的说明）；9/17 那次按实际排课范围确认的是 **8/3 起**。两者对当前 8 月排课都成立（首课为 8/3）；若你要 8/3 起，界面「人工月 / 工资周期」改一次即可，改动会成为该月份的新版本并保留旧版本。

## 测试环境收口（ENV）

| # | 项目 | STATUS | 根因 | 处理 |
|---|---|---|---|---|
| ENV-01 | launcher 健康探针 | PASS | 探针用会读代理变量的 opener；开发 shell / 沙箱导出 `HTTP_PROXY` / `ALL_PROXY` 时，127.0.0.1 的请求被本地代理接走，空闲端口被误判成 `foreign`（`test_launcher.py` 恰好 3 项假失败） | 探针改用显式禁用代理的 opener；新增 2 项回归（带代理时空闲端口仍判 absent、带代理仍能识别本服务） |
| ENV-02 | pytest 临时根 | PASS | 环境通过 `PYTHONPATH` 注入的 shim 拦截 `mkdir` 且不认 `exist_ok`，导致 `tmp_path` 大面积 `PermissionError` | 项目统一测试入口 `tools/run_tests.sh`：剥离 shim 条目、清代理变量、临时根固定在仓库内并加入 `.gitignore` |
| ENV-03 | 导出时间戳路径依赖（真实缺陷） | PASS | 导出时间戳在**整条路径**上做正则；本机仓库目录名以 `-20260913` 结尾，目录片段先命中但没有时分 → 函数返回“没有时间戳” → “导出污染/升学升级”规则失效。同一份代码只因临时目录在仓库内就失败 | 改为先取文件名，仅在文件名无可用时间戳时才回退到所在目录；不引入 `pathlib`（该模块刻意不接触文件系统）。新增 2 项确定性回归 |
| ENV-04 | GitHub CI | PARTIAL | 仓库没有 CI，push 后无法独立显示通过/失败 | 新增 `.github/workflows/tests.yml`（Python 3.12 + 同一个 `tools/run_tests.sh`）；本地同一命令已跑通，GitHub 首次结果待 push 后确认 |
| ENV-05 | 测试卫生 | PASS | 为跑测试随意 `git clean` / `stash` 会破坏工作区 | 本轮未执行任何 `git clean`；对照旧行为时使用仓库旁的独立 worktree，用完即删；定向通过后才跑全量（只跑一次） |

## 本轮回归

- 统一入口：`sh tools/run_tests.sh`
- 结果：**495 passed / 0 failed**（本轮开始前基线 451 passed / 0 failed；新增 44 项定向测试）
- 定向顺序：先跑本任务相关定向测试 → 通过后再跑一次全量。

## 剩余未解决 / 待真实 UAT

1. **真实界面点击回归未做**（本记录里所有 PARTIAL 的共同原因）：人工月卡片保存、基本工资整表导入全流程、续费「已导入 / 已确认」两态、缺模板跳转与返回、每个关键按钮的忙碌与反馈，都需要在浏览器里真点一遍才能写 PASS。
2. **ENV-04**：GitHub CI 首次运行结果待确认。
3. **2026-08 人工月口径**：8/1 起（本轮记录）还是 8/3 起（9/17 实际排课范围），需你确认一次。
4. 自动核算后 8 月 Run 仍有 45 条真实业务阻塞（基本工资未录、续费未确认、退费等），属于正常待办，不是本轮缺陷。

> 边界：本任务到此停止开发，不再扩展 Dashboard、教师端、管理评价、批注体系等外围功能。
