---
name: salary-verification
description: "根据排课记录生成工资核对表，含年级提取、SWITCH公式写入、薪资计算核验。适用于每月制作排课记录sheet、工资核对sheet并验证该档每小时金额(AE列)。"
agent_created: true
---

# 工资核对表制作流程

从排课记录源文件生成工资核对表.xlsx，含"排课记录"和"工资核对"两个sheet。

> 本 skill 是一个**包**：除本文档外，还包含可直接运行的脚本（`scripts/`）、空白模板（`assets/`）与查表数据（`references/`）。运行环境需 Python 3.9+ 且安装 `pandas` / `openpyxl` / `xlrd`（系统 `/usr/bin/python3` 已具备）。

## 〇、每月原材料清单（开工前先核对齐全）

**A. 用户每月提供的（4 份）：**
1. **排课列表**（xls/xlsx，如 `排课列表_06月29日到08月02日_*.xls`）→ 生成排课记录/核对表/课时生产
2. **两张填报表**：`2026数学组X月薪资表.xlsx` + `2026年X月份理化组薪资.xlsx`（填报课时、星级、批注）
3. **续费+推荐数据**（`2026年度续费+推荐数据(*).xlsx`，含各月续推）→ AH/AI/AJ/AK
4. **退费统计表**（`副本宣城二校退费统计表2026年.xls`，P列=负责教师/Q列=人头/R列=业绩）→ AN

**B. 权威/参考文件（平时备着）：**
5. **5月薪资表**（`2026数学&理化组5月薪资表.xlsx`）→ **星级权威**（数学组提交的星级常错，以它为准）
6. **组长绩效标准**（doc，Documents/06-考核管理/）→ 管理绩效完整规则（用户尚未教完整版）
7. **数据统计表**（数学组/理化组周报表）→ 组课时生产页（月度生产/平均课次）

**C. skill 自带（不用用户提供）：** 工资核对表模板.xlsx、学生年级查表.csv

⚠️ 若某月缺续推/退费/填报表 → 先问用户，不要用空数据硬填。

## 〇、包结构

```
工资核对表制作/
├── SKILL.md                       # 本文件：完整流程与规则
├── scripts/
│   ├── _grade_extractor.py        # 年级/班型/学科提取 + SWITCH 公式构造（被下面两个 import）
│   ├── import_paikeshi.py         # 排课记录源文件 → 工资核对表（排课记录 sheet）
│   └── verify_ae_af.py            # 验证薪资表 AE(课时单价) / AF(总课时费) 列
├── assets/
│   └── 工资核对表模板.xlsx         # 已验证可用的空白模板（复制后重命名使用）
└── references/
    ├── 学生年级查表.md             # 赠换特批生 → 年级（含查找逻辑说明）
    ├── 学生年级查表.csv            # 同上，机器可读（脚本读取此文件）
    ├── ae_tier_rules.md           # AE 档位 + 星级加成规则（独立成表）
    └── star_override_template.csv # 权威星级覆盖表模板（姓名,星级）
```

## 一、脚本用法（核心两步）

### 步骤 A：生成工资核对表
```bash
python3 scripts/import_paikeshi.py \
    --source  排课列表_06月01日到06月28日.xls \
    --template assets/工资核对表模板.xlsx \
    --output  工资核对表6月.xlsx
```
- 自动识别新旧两版列名；过滤"已上课"且"实到>0"；提取年级/学科/班型；写入 F/G/H 的 SWITCH 公式
- 写入前自动备份 `--output`（除非 `--no-backup`）；保存时设 `fullCalcOnLoad` 解决 Excel 打开公式显示 0
- 输出末尾会列出**无法识别年级**的记录，需人工核对或补充 `references/学生年级查表.csv`

### 步骤 B：验算薪资表课时单价
```bash
# 表内 F 列星级可信时：
python3 scripts/verify_ae_af.py --file 2026数学组6月薪资表.xlsx --sheet 工资核对

# 表内星级不可信、以口头确认的星级为准时（如 6 月表）：
python3 scripts/verify_ae_af.py --file 2026数学组6月薪资表.xlsx \
    --sheet 工资核对 --star-override references/star_override_template.csv

# 管理/底薪行不参与课时费规则，跳过：
python3 scripts/verify_ae_af.py --file xxx.xlsx --skip-keywords 管理,底薪
```
- 输出 AE / AF 不匹配清单；**只报告、不修改**，特殊个案（AF 为人工录入值）需人工确认

> ⚠️ 已知：4 月表 28 人中 26 人 AF 完全吻合规则，仅 `任勇`(TRMT，AF 为人工值) 与 `杨宇宸`(AF 人工值) 偏离——属人工录入特例，脚本会标出供复核，勿直接覆盖。



## 一、读取源文件并过滤

- 源文件格式：xls 或 xlsx（OOXML格式），用 `pd.read_excel(..., engine='openpyxl')`
- 过滤规则：
  - 只保留 `上课状态 == '已上课'` 的记录
  - 排除 `实到 == 0`（或列名为"实到人数"）的记录
  - 排除 `实到` 为空的记录
- **注意列名差异**：不同月份的排课记录列名可能不同
  - 旧版本（5月部分）：列名含 `班级名称`、`任课老师`、`课程所属班型`、`课程所属年级`、`实到人数`
  - 新版本（6月以后）：列名含 `上课班级`、`教学形式`、`上课科目`、`任课老师`、`实到`
  - 6月新文件可能多出 `上课科目` 列（直接从系统导出含科目），此时优先使用此列作为学科来源，并清理前缀：`str.replace(r'^\d+-', '', regex=True)`

## 二、列映射规则

| 工资核对表列 | 数据来源 |
|---|---|
| A - 任课老师 | 源文件 `任课老师` 列 |
| B - 年级 | 从班级名称提取（见下方规则） |
| C - 学科 | 优先用 `上课科目` 列��清理数字前缀后），否则从班级名称括号提取 `\((\d+-)?([^)]+)\)` |
| D - 课程所属班型 | 从 `教学形式` 或 `课程所属班型` 映射（见下方） |
| E - 实到人数 | 源文件 `实到` 或 `实到人数` 列 |
| F - 班课绩效 | SWITCH公式 |
| G - 年级系数 | SWITCH公式 |
| H - 人数系数 | SWITCH公式 |

### 班型映射
- `一对一` → `1对1`
- `集体班` → `小班`（不论班级名称中是否含"小班"）
- `一对多` → `1对2`
- `10人班`、`6人班`、`8人班` → `小班`

### 年级提取规则（优先级从高到低）
1. 班级名称含 `领航` → 年级 `领航伴学`
2. 暑假衔接班 → 按**当前在读年级**（2026-07 用户确认）：`小升初`→六年级、`初升高`→九年级、`七升八`→七年级、`八升九`→八年级；**`幼小衔接`→一年级**（2026-08-04 用户确认按一年级计，影响李娜等幼小衔接班课）
3. 班级名称含年级关键词：`一年级`~`六年级`、`七年级`~`九年级`、`初一`~`初三`、`高一`~`高三`；**`初一/初二/初三` 统一归一为 `七年级/八年级/九年级`**（模板表头用七年级系，SWITCH 公式不认初一系）
4. 赠送/换购/特批类 → 从班级名称下划线后提取学生姓名，查 `references/学生年级查表.csv`（已收录 72 名学生，含暑假赠课生）
5. 6月起源文件无 `课程所属年级` 列，全部从班级名称提取

## 三、SWITCH公式（F/G/H列）

### F列（班课绩效）
```
=_xlfn.SWITCH(B{row},"领航伴学",0.6,"一年级",0.85,...,"高三",1.35,"雅思",1.5,"托福",1.5,0)
*_xlfn.SWITCH(E{row},1,0.8,2,1,3,1.2,4,1.4,5,1.7,6,1.9,7,2.1,8,2.3,9,2.5,10,2.7,0)
*_xlfn.SWITCH(D{row},"小班",1,"1对1",0,"1对2",1.2)
```

### G列（年级系数）
```
=_xlfn.SWITCH(B{row},"领航伴学",0.6,"一年级",0.85,...,"高三",1.35,"雅思",1.5,"托福",1.5,0)
```

### H列（人数系数）
```
=_xlfn.SWITCH(E{row},1,0.8,2,1,3,1.2,4,1.4,5,1.7,6,1.9,7,2.1,8,2.3,9,2.5,10,2.7,0)
```

## 四、工资核对sheet公式

### D-O列（各年级1v1课时 = SUMIFS × 3）
```
=(SUMIFS(排课记录!$E:$E,排课记录!$D:$D,$C$2,排课记录!$A:$A,$C{row},排课记录!$B:$B,{grade_cell}))*3
```
- `$C$2` = "1对1"（班型条件）
- `$C{row}` = 该行教师姓名
- `{grade_cell}` = 对应年级的表头（如 `D$2`="一年级"）

### Q列（折算小时数）
```
=(D{row}+E{row}+...+I{row})/3*2*0.85+(J{row}+K{row})/3*2*0.9+...
```

### R/U列（填报 - 推荐**内嵌填报数据 sheet**做内部 VLOOKUP，跨文件引用在 WPS/预览会失效）
> 2026-08-04 实测：跨文件 VLOOKUP 在 WPS 桌面版也无法更新（弹"链接无法更新"），预览里更算不出。**推荐做法：把填报数据内嵌为隐藏 sheet，公式查本表**，最稳。

**做法：**
1. 新建隐藏 sheet "填报数据"，列：姓名(clean 后)、1对1折算(AA)、班课折算(AC)；写入所有有填报的教师（约 29 行：数学组+理化组+徐良琴单独表数据合并，徐良琴单独表覆盖数学组空行）
2. 工资核对 sheet R/U 列写最简单的内部 VLOOKUP（不用通配符，因为姓名两边都 clean）：
   ```
   =VLOOKUP(C{row},填报数据!$A:$C,2,0)   # 1对1填报
   =VLOOKUP(C{row},填报数据!$A:$C,3,0)   # 班课填报
   ```
3. 把 "填报数据" sheet 设为隐藏（`ws.sheet_state='hidden'`），预览/WPS 都能立刻算出数字

**回退方案：按组写跨文件 VLOOKUP**（若必须保持链接到外部填报表）：
```
# 数学组：=VLOOKUP(C{row},'/Users/macos/Desktop/7月工资表制作与核查/[2026数学组7月薪资表.xlsx]Sheet1'!$C$5:$AC$23,25,0)
# 理化组：=VLOOKUP(C{row},'/Users/macos/Desktop/7月工资表制作与核查/[2026年7月份理化组薪资.xlsx]教学部'!$C$5:$AC$15,25,0)
```
⚠️ **跨文件链接必须用完整绝对路径**（2026-08-04 实测）：只写 `[文件名]Sheet!` 相对引用，WPS 找不到源文件位置 → 全员 #N/A；带 `'/完整/路径/[文件名]Sheet'!` 前缀后 WPS 可直接定位。Excel 一般能按相对路径解析，但 WPS 需要绝对路径（或先打开源文件让 WPS 自动补全路径）。
⚠️ 此方案在预览面板里仍然算不出数字，需 WPS 桌面版打开。

### S/V列（差值）
```
=Q{row}-R{row}
=T{row}-U{row}
```

### T列（班课）
```
=SUMIFS(排课记录!F:F,排课记录!A:A,C{row})*2
```

## 五、保存注意事项

### fullCalcOnLoad 修复
openpyxl 写入大量公式后，Excel 打开可能不自动重算（公式显示0），需在保存前设置：
```python
wb.calculation.fullCalcOnLoad = True
```

### 多余行清理
写完新数据后删除多余行：
```python
target_last = len(src) + 1
if ws.max_row > target_last:
    for row in range(ws.max_row, target_last, -1):
        ws.delete_rows(row)
```

### ⚠️ 跨表复制/合并：必须带批注（2026-08-04 用户要求）
- 合并/复制行时，除值+公式+样式外，**必须复制单元格批注**（comment），否则老师写的班课明细/星级/社保备注会丢
```python
from openpyxl.comments import Comment
# 复制批注（按姓名匹配目标行；已存在的跳过）
for row in src_ws.iter_rows():
    for c in row:
        if c.comment and c.comment.text.strip():
            dst = dst_ws.cell(target_row(c.row), c.column)
            if not dst.comment:
                dst.comment = Comment(c.comment.text, c.comment.author)
```

### ⚠️ 删除行后公式行号不会自动修正（openpyxl 不解析公式）
`ws.delete_rows(r)` 只移动单元格对象，**公式字符串里的行号引用不会变**，需正则修正：
```python
def fix_formula(f, old_from):
    def rep(m):
        n = int(m.group())
        return str(n - 1) if n >= old_from else m.group()
    return re.sub(r'(?<=[A-Za-z])\d+', rep, f)   # 只匹配 字母+数字 引用，不动小数
```

### ⚠️ 薪资表教师名单以「工资核对表」为准（2026-08-04 用户确认）
- **工资核对表（排课记录）里没有的老师 = 本月没课 = 不出现在薪资表**
- 合并总表前，先按核对表名单核对；核对表没有的老师（如 7月王星）直接从薪资表删除
- 排课源文件"任课老师"为空/无记录也可佐证

### ⚠️ 续推数据版本校验：新旧版对比防漏人（2026-08-04 教训）
- 续费+推荐数据可能有多版（如 7月：桌面版(1)8月2日 新版 / 下载版(2)7月19日 旧版），**新版修订时常会"换名单"导致老师被误删**
- 填续推数据前，先对比新旧版当月名单：新版缺了谁 → 大概率是修订误删（如 7月廖永翠：旧版有班课20→30，新版整行消失）
- 对比方法：同一老师逐月核对两版数据是否一致，仅当月缺失 = 疑点，需人工确认再决定填 0 还是补旧版值
- 只有用户确认后才能补；默认以用户选定版本为准，缺失的填 0 并提示

### ⚠️ 清空单元格的坑（2026-08-04 踩过）
`ws.cell(r, c, None)` 是 **no-op**（None 是默认值，等于没赋值），清空必须：
```python
ws.cell(r, c).value = None
if (r, c) in ws._cells:
    del ws._cells[(r, c)]   # 保险起见从内部字典移除
```
写值/写公式用 `ws.cell(r, c, value)` 或 `ws.cell(r, c).value = value` 都可以。

## 六、薪资表AE列验证

### 该档每小时金额（AE列）规则
- `AE` = `基础档位(查AD)` + `星级加成(查F列教师级别)`
- **有效期至2026年9月**，10月可能调整

**基础档位表（AD → 基础金额）：**
| AD范围 | 基础金额 |
|--------|---------|
| 0-30 | 0 |
| 31-60 | 30 |
| 61-80 | 32 |
| 81-100 | 34 |
| 101-130 | 36 |
| 131-160 | 37 |
| 160以上 | 38 |

**星级加成：**
| 星级 | 加成 |
|------|------|
| 一星/二星 | +0 |
| 三星 | +5 |
| 四星 | +10 |
| 五星 | +15 |
| 六星 | +20 |

**教师级别提取规则：** F列含"几星"即取对应星级，TR/TRS/TRST/TRMT为前缀不影响星级判定。

### AF列（总课时费）公式规则
- 非TRMT老师：`=(AD-30)*AE`（前30小时不计入课时费）
- TRMT老师：`=AD*AE`（全额计入，无减免）

### 薪资表关键列对照
- 列C：姓名
- 列F：教师级别（含星级）
- 列AD：最终授课小时数据
- 列AE：该档每小时金额
- 列AF：总课时费

## 七、备份与数据安全
- 每次修改数据前必须先创建备份文件：`shutil.copy2(原文件, 原文件_备份.xlsx)`
- 修改后告知用户具体改动（哪一行、哪一列、原值→新值）
- 修改前备份到文件系统，不要依赖回滚操作

## 八、学生年级查表
当遇到赠送/换购/特批类班级名称时，从班级名称提取学生姓名后查 `references/学生年级查表.md`。未找到的记录返回空字符串，待用户确认后补充。

## 九、管理绩效（管理团队奖 AM列）（2026-08-04 归纳）
**适用对象**：组长/教学负责人（如李凡，管理岗行单独在总表末尾，不参与排课）
**公式**：`AM = 数学组生产课时×单价 + 理化组生产课时×单价 + 非个人续费推荐 − 非个人退费`
- **生产单价（按绩效标准）**：旺季（暑假）**平均课次>16次 → 1元/KS**；淡季（平均课次≤16）→ 0.8元/KS（2026-08-04 用户确认）
  - 平均课次 = 组月总平均课次（月周均）；判断时两组分别看
- **非个人**：指组长个人之外的续推/退费（组内其他人的）
- 批注格式（参考6月总表，AM单元格写批注）：
```
理化X月平均课次Y，生产绩效系数Z元/KS（旺季>16次/淡季0.8）
数学X月平均课次W，生产绩效系数Z元/KS
非个人续费推荐 N
非个人退费 M
合TOTAL=数学生产+理化生产+续推-退费
```
- 例（7月李凡）：=6141+3146+3757.7-804.06（数学6141、理化3146课时，均>16次→1元/KS；非个人续推3757.7、退费804.06）

## 十、课时生产与平均课次（组课时生产页）（2026-08-04 归纳）
**数据源**：排课列表 xls（上课时间取字符串前10位转日期；周划分：第1周=起日+7天×n）
**口径（用户确认）**：
- **课次 = 排课表行数**（1对1一行1次、班课一行1次；1对2归班课）
- **1对1生产KS = 课次×3**（每节3课时）
- **班课生产KS = 实到×3**（每生每节3课时）
- **生产课时（周） = 1对1KS + 班课实到**（= 1对1课次×3 + 班课实到；表里班课KS列是×3、生产课时里班课按实到）
- **平均课次 = (1对1课次 + 班课课次) ÷ 教师数**（教师数按统计表填：数学18、理化按实际）
- 生数暂不算；平均课时（D/E列）口径未定，保留表值
- 填写：统计表「组课时生产」页 B/C(生产月/周)、F/G(平均课次月周均/周均，月周均=累计课次÷教师数÷周数)、I(1对1KS)、M(班级数)、N(班课KS)、O(师课次=班课课次)、Q(教师数)
- 数学组统计表是 .xls：用 xlrd + xlutils.copy 保留格式写入（sheet index 5=组课时生产）；理化 .xlsx 用 openpyxl

## 十一、AE验证补充：反推法（星级以核过月份为基准）
- F列星级可能不准（如徐良琴写二星实际三星、胡长春写五星实际六星）→ 别直接用F列
- **反推法**：用上一月（用户核过）的 AE 反推星级加成（加成 = AE − 档位(AD)），再算本月应AE = 档位(本月AD) + 加成
- ⚠️ AD 在档位边界（如80.96 vs 81）时反推会偏差，需结合 F 列/实际复核
